# yaml-language-server: $schema=https://moonrepo.dev/schemas/toolchain.json
$schema: 'https://moonrepo.dev/schemas/toolchain.json'

# Enable moon's Rust toolchain. Version/edition are sourced from Cargo
# (Edition 2021); cargo + the workspace Cargo.lock drive task hashing.
rust: {}
